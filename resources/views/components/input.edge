<div>
  <label for={{ id }} class="block text-sm font-medium leading-5 text-gray-700">{{ label }}</label>
  <div class="mt-1 relative rounded-md shadow-sm">
    <input id={{ id }} type={{ type }} name={{ name }} class="form-input block w-full sm:text-sm sm:leading-5" placeholder={{ placeholder ? placeholder : '' }}>
  </div>
  @if(error)
    <p class="mt-2 text-sm text-red-600" id="email-error">{{ error }}</p>
  @endif
</div>