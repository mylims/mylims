@layout('layouts/admin')

@section('content')
  <form action="{{ route('AdminsController.auth') }}" method="POST">
    <h1 id="message">System configuration</h1>
    <small id="smallMessage"> </small>
    <div class="field">
      <label for="body">Admin password </label>
      <input
        type="password"
        name="password"
        id="password"
        placeholder="Admin password"
      />
    </div>

    <button type="submit">Login</button>
  </form>

  @if(flashMessages.has('error'))
    <div class="error">{{ flashMessages.get('error') }}</div>
  @endif
@endsection
